PORT=5000
NODE_ENV=development

MONGO_URI=mongodb+srv://zaalimaproject_db_user:zaalima%40123@cluster0.trajix2.mongodb.net/zaalimadb?retryWrites=true&w=majority

JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

REDIS_URL=redis://localhost:6379
# Redis cache TTLs (seconds) — all have sensible defaults if omitted
# REDIS_TTL_BOARD=300
# REDIS_TTL_CARD=180
# REDIS_TTL_MEMBERS=600
# REDIS_TTL_LISTS=300

CLIENT_ORIGIN=http://localhost:3000

INVITE_TOKEN_EXPIRES_HOURS=48